package org.example;

import java.util.Scanner;

public class Lesson11_Practice {
    public static void main(String[] args) {
        //Задача 3. Сумма положительных чисел в массиве
        //Тип метода: возвращает значение int
        //
        //📝 Условие:
        //Напишите метод sumPositive(int[] arr), который принимает массив целых чисел и возвращает сумму всех
        // положительных элементов (т.е. строго больше 0).
        //Ноль и отрицательные числа игнорируются.
        //
        //В методе main создайте массив из 6 чисел (можно задать вручную или запросить у пользователя) и выведите результат в формате:
        //Сумма положительных чисел: X

        /* Scanner sc = new Scanner(System.in);
        int[] arr = new int[6];
        System.out.println("Введите 6 целых чисел:");
        for (int i = 0; i < arr.length; i++) {
            arr[i] = sc.nextInt();// 0 1 2 3 4 5
        }

        int result = sumPositive(arr);
        System.out.println("Сумма положительных чисел: " + result);
        */

        //Задача 4. Вывод всех чисел, кратных 3 или 5
        //Тип метода: void (ничего не возвращает)
        //
        //📝 Условие:
        //Напишите метод printMultiplesOf3Or5(int[] arr), который принимает массив целых чисел и выводит все элементы,
        // которые делятся на 3 или на 5 без остатка (используйте оператор %).
        //Каждое подходящее число выводится с новой строки.
        //Если таких чисел нет — ничего не выводится (дополнительное сообщение не требуется).
        //
        //В main создайте массив из 7 чисел (например, {10, 7, 9, 15, -5, 8, 3}) и вызовите метод.

        int[] array = {10, 7, 9, 15, -5, 8, 3};
        printMultiplesOf3Or5(array);
    }

    static void printMultiplesOf3Or5(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            if ((arr[i] % 3 == 0) || (arr[i] % 5 == 0)) {
                System.out.println(arr[i]);
            }
        }

//        for(int num : arr){
//
//        }
    }


    static int sumPositive(int[] arr) {
        int sum = 0;
        // 4 3 2 7 8 9 — значения элементов массива arr.length равна кол-во элементов в массиве 6
        // 0 1 2 3 4 5 — i индексы (порядковые номера элементов)
        for (int i = 0; i < arr.length; i += 1) {
            if (arr[i] > 0) {
                sum += arr[i];//sum = sum + arr[i]
            }
        }
        return sum;
    }
}


//ДЗ
// Задача 1 - Задача 1. Простое ли число?
//Условие:
//Напишите метод isPrime(int n), который проверяет, является ли число простым (делится только на 1 и на само себя).
// Метод должен вернуть true, если число простое, и false — иначе.
//Числа ≤ 1 считаются непростыми.
//
//В методе main запросите у пользователя число и выведите результат в формате:
//Число X [является/не является] простым.

      /*  Scanner sc = new Scanner(System.in);
        System.out.println("Введите число");
        int n = sc.nextInt();
        if (isPrime(n)) {
            System.out.println("Число " + n + " является простым");
        } else {
            System.out.println("Число " + n + " не является простым");
        }

        // Задача 2. Вывод всех чётных чисел из массива
        //Условие:
        //Напишите метод printEvenNumbers(int[] arr), который принимает массив целых чисел и выводит все чётные элементы
        // в порядке их следования. Каждое число — с новой строки.
        //
        //В main создайте массив из 5–7 чисел (можно запросить у пользователя или задать вручную) и вызовите метод.


        System.out.println("Задайте размер массива (от 5 до 7)");
        int[] arr = {1, 20, -8, 3, 6, 84};
        printEvenNumbers(arr);
    }

    static boolean isPrime(int n) {
        // Числа <= 1 не являются простыми
        if (n <= 1) {
            return false;
        }
        // 2 — единственное чётное простое число
        if (n == 2) {
            return true;
        }
        // Все остальные чётные числа — составные
        if (n % 2 == 0) {
            return false;
        }

        // Проверяем нечётные делители от 3 до √n
        for (int i = 3; i * i <= n; i += 2) {
            if (n % i == 0) {
                return false;
            }
        }

        return true;
    }


    static void printEvenNumbers(int[] arr) {
        for (int i : arr) {
            if (i % 2 == 0) {
                System.out.println(i);
            }
        }
    }
}
*/

//Допустим, у нас есть число 36.
//Мы хотим понять, делится ли оно на что-то, кроме 1 и самого себя.
//Давайте выпишем все пары множителей, которые дают 36:
//1 × 36
//2 × 18
//3 × 12
//4 × 9
//6 × 6 ← вот он — квадратный корень! (√36 = 6)
//9 × 4
//12 × 3
//18 × 2
//36 × 1
//Заметьте: после √36 пары просто повторяются в обратном порядке, следовательно и проверять мы можем только до √36
